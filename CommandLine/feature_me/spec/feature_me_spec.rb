require 'spec_helper'

describe FeatureMe do
  it 'has a version number' do
    expect(FeatureMe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
